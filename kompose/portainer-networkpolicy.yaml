apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  creationTimestamp: null
  name: portainer
spec:
  ingress:
    - from:
        - podSelector:
            matchLabels:
              io.kompose.network/portainer: "true"
  podSelector:
    matchLabels:
      io.kompose.network/portainer: "true"
